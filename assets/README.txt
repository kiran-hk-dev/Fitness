Place app icons here:
- icon.png (1024x1024)
- splash.png
- adaptive-icon.png (Android)

Placeholders are fine for `expo start`. For store builds, replace with real assets.
Exercise demo videos/GIFs live in Supabase Storage `exercise-media` — see supabase/migrations/0001_init.sql.
UI uses thumbnail_url / media_url so production media can be swapped without code changes.
